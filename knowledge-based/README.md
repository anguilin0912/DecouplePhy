This folder contains the artifact to Sec. 6.1.3. To reproduce the results of Tab. 3, execute `run.sh`. The evaluation results are stored in `output.json`.

The implementation of the IIDS is located at `code/ipal-ids-framework/ids/Knowledge-based/ExpertInvariants.py`.